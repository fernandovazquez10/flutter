import 'package:flutter/material.dart';


class TrendsView extends StatelessWidget {
  const TrendsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text('Treanding movies...'),
    );
  }
}
